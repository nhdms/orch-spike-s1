name: review-codex-legit-source
on:
  pull_request:
    types: [opened, synchronize]
permissions:
  checks: write
  pull-requests: read
jobs:
  legit-no-op:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is what SHOULD be the only source of review-codex checks"
